cmake_minimum_required(VERSION 2.6)
project(chatroomv2)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -pthread")
aux_source_directory(. SRC)
add_executable(chatroomv2 ${SRC})
install(TARGETS chatroomv2 RUNTIME DESTINATION bin)
